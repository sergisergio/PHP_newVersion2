<form action="index.php?c=comment&t=addSubComment&id={{ post.id }}&commentId={{ comment.id }}" method="post">
    <input type="hidden" name="user_id" value="{% if session.admin %}{{ session.admin.id }}{% elseif session.user %}{{ session.user.id }}{% endif %}">
    <input type="hidden" name="post_id" value="{{ post.id }}">
    <input type="hidden" name="comment_id" value="{{ comment.id }}">
    <div>
    <label for="content">Répondre</label>
    <br />
    <textarea id="content" name="content"></textarea>
    </div>
    <div class="text-center">
    <input class="btn btn-default validate" type="submit" />
    </div>
    <div>
    <input type="hidden" name="add_subcomment_token" id="add_subcomment_token" value="{{ add_subcomment_token }}" />
    </div>
</form>
